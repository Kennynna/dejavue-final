export type IProduct = {
    name: string
    brand: string,
    price: number,
    image: string,
    gender: string,
    volume: string,
    rating: number,
    description: string,
    featured: boolean,
}